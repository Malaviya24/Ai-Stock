8. Nifty & Nifty Bank me Hunmagenius Method

Excellent Rajesh 🔥
Now we convert this **Weekly ETF Contrarian System** into a clean, exact, production-ready trading engine.

You gave:

* Excel screenshot (weekly accumulation logic)
* Theory explanation
* Capital management rule (12 parts)
* No stop loss
* 3% fixed profit booking
* ETF only (NiftyBees etc.)

Now I will:

1️⃣ Extract exact algorithm
2️⃣ Analyze your sheet logic
3️⃣ Convert to implementable rules
4️⃣ Generate production-ready prompt
5️⃣ Provide Next.js + Yahoo implementation structure

---

# 📊 PART 1 — IMAGE ANALYSIS (Your Excel Sheet)

From your sheet:

Columns:

* Date
* Open
* High
* Low
* Close
* Candle Formation
* Buy Quantity (42 units example)
* Investment with brokerage
* Total Quantity
* Total Investment
* Average Price
* Target 3%

---

## 🔎 What is Actually Happening?

Whenever weekly candle = **Bearish**

He buys fixed quantity (example 42 units).

Accumulation continues week after week.

Average price keeps recalculating.

When market recovers and price >= 3% above average price → Book entire position.

---

## 🔥 Important Observation

There is:

❌ No stop loss
✔ Only profit exit
✔ Capital deployed gradually
✔ 12-part max allocation

---

# 🎯 PART 2 — EXACT SYSTEM LOGIC (Clean Rules)

---

# 🔷 STEP 1 — Universe Restriction

Apply ONLY to:

ETF universe:

* NiftyBees (NIFTY ETF)
* BankBees
* FinNifty ETF
* Midcap ETF
* Sensex ETF

Ignore individual stocks.

---

# 🔷 STEP 2 — Weekly Candle Definition

Weekly structure:

Week = Friday Open → Thursday Close
(Or Monday–Friday standard weekly if using Yahoo)

Weekly OHLC:

```javascript
weeklyOpen  = first trading day open
weeklyHigh  = highest high
weeklyLow   = lowest low
weeklyClose = last trading day close
```

---

# 🔷 STEP 3 — Candle Type

```javascript
if (weeklyClose < weeklyOpen)
   candle = "Bearish"
else
   candle = "Bullish"
```

---

# 🔷 STEP 4 — Entry Rule (Contrarian)

BUY only when weekly candle is Bearish.

Why?

Fear week → Accumulate.

---

# 🔷 STEP 5 — Capital Management (12 Parts Rule)

If total capital = ₹1,20,000

Each week deploy:

```text
capital_per_trade = total_capital / 12
```

Only one installment per week.

Max installments = 12.

If 12 filled → Stop buying.

---

# 🔷 STEP 6 — Position Tracking

Maintain:

* totalQuantity
* totalInvestment
* averagePrice

Formula:

```javascript
averagePrice = totalInvestment / totalQuantity
```

---

# 🔷 STEP 7 — Exit Rule

Target:

```javascript
targetPrice = averagePrice × 1.03
```

If:

Current price >= targetPrice

→ Sell entire position
→ Reset capital usage

---

# 🔷 STEP 8 — No Stop Loss

Hold until:

Either:

* 3% profit achieved
  OR
* Capital exhausted (12 parts used)

No downside exit rule.

---

# 📈 PART 3 — EDGE CASES (From Your Excel Notes)

You mentioned:

"When Open = High we are not able to book profit because ETF opens high but orders not executed."

So real logic:

If:

Open >= targetPrice

Exit at open price
(Not waiting for execution delay)

---

# 🚀 MASTER IMPLEMENTATION PROMPT

(Weekly ETF Contrarian Engine)

Use this in OpenAI or backend rule engine.

---

```text
You are a systematic weekly ETF trading engine.

IMPORTANT:
This strategy applies ONLY to NSE ETFs tracking major indices:

- NIFTY ETF
- BANK ETF
- FINNIFTY ETF
- MIDCAP ETF
- SENSEX ETF

Do NOT apply to individual stocks.

----------------------------------------------------
STEP 1 — FETCH DATA
----------------------------------------------------

Fetch 1 year historical daily data.

Aggregate into weekly OHLC.

----------------------------------------------------
STEP 2 — CANDLE IDENTIFICATION
----------------------------------------------------

If weeklyClose < weeklyOpen:
    candle = "Bearish"
Else:
    candle = "Bullish"

----------------------------------------------------
STEP 3 — ENTRY RULE
----------------------------------------------------

If candle == "Bearish":
    Invest fixed capital installment
    (total capital divided into 12 equal parts)

----------------------------------------------------
STEP 4 — POSITION TRACKING
----------------------------------------------------

Maintain:

totalQuantity
totalInvestment
averagePrice = totalInvestment / totalQuantity

----------------------------------------------------
STEP 5 — EXIT RULE
----------------------------------------------------

targetPrice = averagePrice × 1.03

If currentPrice >= targetPrice:
    Sell entire quantity
    Reset position

No stop loss.

----------------------------------------------------
STEP 6 — CAPITAL CONTROL
----------------------------------------------------

Maximum 12 installments allowed.

If 12 installments filled:
    Stop further buying.

----------------------------------------------------
FINAL OUTPUT
----------------------------------------------------

Return JSON:

{
  symbol: "",
  weekly_candle: "",
  installment_used: "",
  total_quantity: "",
  total_investment: "",
  average_price: "",
  target_price: "",
  current_price: "",
  position_status: "Accumulating | Target Hit | Waiting",
  unrealized_profit: ""
}
```

---

# 🏗 NEXT.JS IMPLEMENTATION STRUCTURE

```
/lib/weeklyAggregator.js
/lib/contrarianEngine.js
/lib/positionManager.js
/app/api/weekly-etf-scan/route.js
```

---

# 📦 Yahoo Finance Example

```javascript
import yahooFinance from "yahoo-finance2";

const data = await yahooFinance.historical("NIFTYBEES.NS", {
  period1: "2024-01-01",
  period2: "2025-01-01",
  interval: "1d"
});
```

Convert to weekly:

```javascript
function aggregateWeekly(data){
   const grouped = {};

   data.forEach(d=>{
      const week = getWeekNumber(d.date);
      if(!grouped[week]) grouped[week] = [];
      grouped[week].push(d);
   });

   return Object.values(grouped).map(days=>{
      return {
         open: days[0].open,
         high: Math.max(...days.map(d=>d.high)),
         low: Math.min(...days.map(d=>d.low)),
         close: days[days.length-1].close
      }
   });
}
```

---

# 🔄 AUTOMATION SCHEDULE

Run weekly:

```javascript
cron.schedule("0 16 * * 4", runWeeklyScan);
```

Thursday 4 PM.

---

# 📊 WHAT YOU NOW HAVE

✔ Weekly contrarian ETF system
✔ 12-part capital safety
✔ 3% fixed booking
✔ No stop loss
✔ Average price tracking
✔ Fully automatable

---

# 🔥 PROFESSIONAL UPGRADE (OPTIONAL)

To improve robustness:

Add filter:

```javascript
if (weeklyClose < 20DMA)
```

OR

```javascript
if (RSI < 40)
```

To avoid shallow pullbacks.

---

Rajesh 🔥

Now you have:

Darvas
SRTV
Monthly Candle
Fundamental Data Bank
Weekly ETF Contrarian

All converted to systematic engines.

If you want next:

1️⃣ Combine all into single portfolio system
2️⃣ Backtest 10 years
3️⃣ Build React dashboard
4️⃣ Add AI signal strength scoring
5️⃣ Add Telegram auto alert

Tell me next level.