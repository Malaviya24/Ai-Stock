{
  "buildCommand": "npx vite build --outDir ../build --emptyOutDir",
  "outputDirectory": "build",
  "installCommand": "npm install --include=dev",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}